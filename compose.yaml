services:
  webhook-api:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      NODE_ENV: production
      PORT: 3000
      LOG_LEVEL: info
      GITHUB_WEBHOOK_SECRET: replace-with-a-strong-shared-secret
    ports:
      - "3000:3000"
