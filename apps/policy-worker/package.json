{
  "name": "@platform-policy-console/policy-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "check": "corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test",
    "lint": "eslint .",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1048.0",
    "@aws-sdk/lib-dynamodb": "^3.1048.0",
    "@platform-policy-console/github-api": "workspace:*",
    "@platform-policy-console/policy-core": "workspace:*",
    "@types/aws-lambda": "^8.10.161",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
